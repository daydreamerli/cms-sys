yarn create next-app cms-sys --typescript

yarn add antd styled-components

yarn add -D @types/styled-components

using sample code in the Form component in antd to generate the Form 

  ** problem antd 配置有问题 webpack 报错
   --导致 title 错位i---dev 环境问题？ ： 换机器从新起项目试试
  ** validateMessages ---api doc--- 
  define defaultValidateMessages then set it in the format

Session2 : Login function:

 ## 老师讲解思路 ： 二次封装登录axios请求 分类信息及错误

 ##难点1： 怎么去按照说明分层封装 
     first: 表单数据验证 ---antd 已进提供
     second：api 请求效验 --crypto add salt
     third：api 返回类型 --- http code 
                       --- 返回数据json 分段id jwt  -- 保存到 localStorage.setItemId / JWT
     
    errHandler : 分类返回

    I: define login types :
        interface LoginFormData {role,email,password,remember}// form submit values
        type LoginRequest-> type of LoginFormData
        interface LoginResponse : {response.json}

    II: create interface : IResponse: type of LoginResponse
         {
             code:number //http status code
             msg:string  
             data?: T   // ? --data or null
         }

    III: set Storage --> using localStorage
         type UserInfo = LoginResponse    //axios-> response-> JSON
         class Storage{
             setUserInfo(userInfo：UserInfo):Promise<string>{ --no need to return
                private userKey = ''
                localStorage.setItem(userKey,JSON.stringify(userInfo)); // setItem(key,value)
                return console.log(`# ${userInfo.userId} user has logged in`)
             }
             getUserInfo():Promise<UserInfo>{
                 try{
                     return JSON.parse(localStorage.getItem(userKey))
                 }catch(err){

                 }
             }
         }

    IV: create class apiService{
         login(loginData: LoginFormData): Promise<IResponse<LoginResponse>>{ 
            // add salt to LoginFormData.password 
            const response = await axiosInstance.post(baseURL,{
                            ...rest,
                            password:AES.encrypt(password,salt).toString
                            })
                            .then((response) => response.data)
                            .catch(err) =>{
                                throw err
                            }  // to-do : set errHandler to deal with all err
            if(response && response.data){
                return response.data as LoginResponse
            }

            return {};
          
        logOut(){}
    }

   *BUG：
       1. validation 报错 '${name}' is not a valid ${type} antd validateMessages 报错
       2. remember 无效
       3. login 201成功 但是localStage 没有存用户数据Router 没有redirect 
               -> 返回定义问题-->需要进一步：errorHandler && showMessage 分类错误并显示消息 -> 课后思考
       4. 返回response后 router无效 : 返回问题 后续代码没有执行

   Next Stage: I: path 定义   II：apiService logout method 


    ##2： api 请求完成后怎么使用路由跳转到对应的role的管理界面
         I : 设置跟ROLE对应的dashboard page
         II: use Router redirect

    ------------------------------------> 课后思考：<------------------------------------------------
    apiService 改进--> 
       I. errorHandler 
          1. 定义错误 什么样的才是错误码，LoginResponse 里面的code: ---除去 2xx/3xx
             isError(code: number): boolean{
                 return !(code.toString().startsWith('2')) || !(code.toString().startsWith('3'))
             } 
        2.  出现错误怎么处理：
            2.1 showMessage --> 
            2.2 errorHandler --> passing 进来 error 返回类型 符合 封装好的IResponse？
                  // 分析错误类型 axios的err.response 还是 IResponse接口返回的错误 还是未知错误
                  message : error.response?.data.msg ?? 'unknown error'  // axios 
                  code : --> error.response?.status
            2.3 showMessage :
     ------------------------------------> <------------------------------------------------
Session3 Homework -dashboard->layout
          I: create dashboard --> manager / student /teacher sub-folder

         II: role : {storage.userInfo.role} ---> redirect to /dashboard/{role}

         III: apiService add method logout :
              
              logout():Promise<?>  返回的type应该是 ？ 怎么样才能回到login页面

Session3 思考：难度进阶！！ --dashboard 设计思路

    I: 怎么根据业务/功能模块 去设计dashboard

    II：UI 上面怎么设计：
     
      I： layout 布局 侧边不动 顶栏 logo + 缩进按钮  +userId + logout LoginButton

      II: 怎么根据ROLE 来调用不同的 components及 布局？

       1 : lib --- layout.tsx 提供公共layout 
               --- 然后设置对应的不同ROLE 的menu ---render 不同的side menu ？？
      难点---》 ？？ router 跳转 login 里面要先router.push('dashboard')
           ----- 那么 dashboard 怎么返回不同的dashboard 路径？？
            ---- 设置一个 根据USER login state来映射路由的functional
                1.如果没有token ---- 回到 login  页面 
                   --》 解决上次关于如何logout后返回 login的问题:
                            --logout 之后删除 userInfo --> LoginState 改变 返回 login路由页面
            
            
           1.1 layout.tsx  放置基本的layout 布局设置及组件
           1.2 缩进 及顶栏组件
           1.3 侧边栏组件--获取登录的不同ROLE 来render不同的侧边栏组件

       2. 后续其他可复用的组件  比如 course event timetable 这些组件

       3: 其余不可复用的内容区的图标等等组件 ------> components / manager / teacher /student folder


    III：怎么设计内容部分 保证可视化图标的可读性及自适应

<----------------------------------------------------------------------------------------------->
1. apiService 需要按照nextjs fetch data 要求写在api 里面
2. Dom 事件模型   --- react event handling ----？？ 
   react.createElement------------ 
3. functional programming --- 
<------------------------------------------------------------------------------------------------>
节流 ----------------&&----------------  防抖
search ??   Lodash .....
顶部面包屑 组件  Breadcrumb
 paginator
 json -------- table 
 
 数据---api 接口---- 返回 ---- table 组件 map  data 
      --swagger :    GET api/student  parameters(page ? limit ? userId ? X)
      search : page ? query? search input

      2. 渲染数据 --- table
        

## 问题：  
     
     I：怎么合理定义student 和 course相关的interface；
     II: 定义好了Paginator 和 QueryParameters 后 怎么这两个接口参数传进apiService里面
        在table里面没有定义paginator --然后由table组件里面传参数给apiService
     III：

   
 

